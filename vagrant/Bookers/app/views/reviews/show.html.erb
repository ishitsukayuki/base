<% if flash[:notice] %>
	<div class="flash">
		<%= flash[:notice] %>
	</div>
<% end %>
<p><strong>Title:</strong><%= @review.title %></p>
<p><strong>Body:</strong><%= @review.body %></p>
<%= link_to "Edit", edit_review_path(@review), class: "edit_#{@review.id}" %>|
<%= link_to "Back", new_review_path(@review), class: "back" %>
